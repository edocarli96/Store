---
import type { Languages } from '~/utils/types';
import 'virtual:windi.css';

export interface Props {
	title: string;
	show_back_to_home?: boolean,
	language: Languages
}


const { title, show_back_to_home, language } = Astro.props;
---

<!DOCTYPE html>
<html class="h-full">

<head>
</head>

<slot/>

</html>
